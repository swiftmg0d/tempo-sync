# @tempo-sync/config

Shared TypeScript configurations for the Tempo-Sync monorepo.

## Configs

- `typescript/base.json` — Base `tsconfig` shared across all packages
- `typescript/worker.json` — Extended config for Cloudflare Workers apps
