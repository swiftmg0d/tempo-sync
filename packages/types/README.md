# @tempo-sync/types

Shared TypeScript types for the Tempo-Sync monorepo.

## Exports

- `api/activity` — Activity-related API types
- `api/strava` — Strava API types
- `api/token` — Token types
- `llm` — LLM request/response types
